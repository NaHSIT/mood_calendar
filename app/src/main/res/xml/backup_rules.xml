<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!--
      Care data shares mood_database with legacy records. Until those stores are
      split and restored with a recoverable key strategy, fail closed: do not
      export the database or session/preferences through Auto Backup.
    -->
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
</full-backup-content>
