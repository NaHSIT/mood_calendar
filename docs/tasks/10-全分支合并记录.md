# 全分支合并记录（2026-09-18）

按用户“先把所有分支全部合并”的要求，将以下模块头整合到 develop，并在验证后同步 main。保留所有原分支及提交历史，不删除 worktree，不使用强制推送。

| 模块 | 纳入的提交 |
|---|---|
| A 公共契约与隐私 | 179a904 |
| B 量表 | b184ff2 |
| C 健康框架 | 57e3d6b |
| D 风险预警 | f2365e3 |
| E 教师工作台 | 5237549 |
| F AA 随访 | 370351a |
| G 集成 | 25d6d99 |

## 冲突处理

- 健康删除接口采用 A 的 `Set<HealthScope>`，应用层将健康指标映射到授权范围；保留 G 对提取样本的授权修订及类型过滤。
- AA 仓储采用 A 的 DAO 构造方式、可配置退出策略和事务内退出/任务取消接口，适配 G 装配代码；保留重复申请的状态检查。退出策略默认值以 `AaExitPolicy` 为准，早期 G 交付中的固定七天描述不再代表当前实现。
- 量表目录和选项采用 B 的类型化版本；G 页面直接读取题目和选项分值/标签。
- 健康页面采用 C 的可观察语言环境读取，并保留 C 新增测试。
- E 教师服务注入 A 的教师随访仓储；保留 E、F 的最新退出审核组件和测试。

## 合并范围与后续验收

本次为分支整合，不代表已解决此前整体检查列出的业务问题。仍需专项验证或处理：

- 预警生成使用的学生代号与注册代号不一致。
- 辅助健康读取失败时，量表预警是否应继续独立执行。
- 授权撤回与原始数据写入之间的原子性。
- 教师安全关注结案、任务完成及 AA 稳定观察的完整业务路径。
- 投递重试与启动恢复、重复提交和草稿恢复。
- 数据库迁移、Keystore 和权限隔离须在设备上运行仪器测试。

各窗口继续开发前，在自己的干净 worktree 获取远程并合并 `origin/develop`。不要将原分支旧代码整体覆盖到统一后的文件。功能分支不会因 main 更新而自动同步。

## 合并验证

在主工作目录使用既有独立 Gradle 缓存执行：

`gradlew.bat --no-daemon --max-workers=1 :app:testDebugUnitTest :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin`

- 最终结果：`BUILD SUCCESSFUL`。
- 实际执行单元测试 60 项，0 失败、0 错误。
- Debug APK 构建、Lint、仪器测试源码编译通过。
- 为适配 A 新增接口，补齐 E 测试替身的 `updateTeacherDisposition` 方法。
- 未运行设备仪器测试，未宣称通过完整业务闭环验收。
